/**
 * Design tokens — single entry point.
 *
 *   import { colors, spacing, sp, fontFamily, fontSize, lineHeight, radius, shadows } from '@/constants';
 *
 * 기존 화면들이 사용하는 `Colors` (대문자, deprecated) 도 함께 re-export.
 */

export * from './colors';
export * from './spacing';
export * from './typography';
export * from './radius';
export * from './shadows';
